{% extends 'base-back.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Panier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panier.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ panier.quantity }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ panier.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_panier_index') }}">back to list</a>

    <a href="{{ path('app_panier_edit', {'idClient': panier.idClient}) }}">edit</a>

    {{ include('panier/_delete_form.html.twig') }}
{% endblock %}
