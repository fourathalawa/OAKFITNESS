<form method="post" action="{{ path('app_programmesportif_delete', {'idprogrammesportif': programmesportif.idprogrammesportif}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programmesportif.idprogrammesportif) }}">
    <button class="btn">Delete</button>
</form>
